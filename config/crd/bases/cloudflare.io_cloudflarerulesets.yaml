---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cloudflarerulesets.cloudflare.io
spec:
  group: cloudflare.io
  names:
    kind: CloudflareRuleset
    listKind: CloudflareRulesetList
    plural: cloudflarerulesets
    shortNames:
    - cfruleset
    singular: cloudflareruleset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.zoneId
      name: Zone ID
      type: string
    - jsonPath: .spec.phase
      name: Phase
      type: string
    - jsonPath: .spec.rules[*]
      name: Rules
      type: integer
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.rulesetId
      name: Ruleset ID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: CloudflareRuleset is the Schema for the cloudflare rulesets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareRulesetSpec defines the desired state of CloudflareRuleset
            properties:
              description:
                description: Description is an optional description for the ruleset
                maxLength: 1000
                type: string
              name:
                description: Name is the ruleset name (defaults to k8s-{metadata.name})
                maxLength: 100
                type: string
              phase:
                description: Phase is the phase in which the ruleset is executed
                enum:
                - http_request_firewall_custom
                - http_request_firewall_managed
                - http_request_transform
                - http_response_headers_transform
                - http_request_late_transform
                - http_request_dynamic_redirect
                - http_request_origin
                - http_response_compression
                type: string
              rules:
                description: Rules is the list of rules in the ruleset
                items:
                  description: Rule defines a single rule in a ruleset
                  properties:
                    action:
                      description: Action is the action to perform when the rule matches
                      minLength: 1
                      type: string
                    actionParameters:
                      description: ActionParameters contains parameters for the action
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      description: Description is an optional description for the
                        rule
                      maxLength: 1000
                      type: string
                    enabled:
                      default: true
                      description: Enabled indicates whether the rule is active
                      type: boolean
                    expression:
                      description: Expression is the Cloudflare Rules Language expression
                      minLength: 1
                      type: string
                  required:
                  - action
                  - expression
                  type: object
                minItems: 1
                type: array
              zoneId:
                description: ZoneID is the Cloudflare Zone ID where the ruleset will
                  be created
                minLength: 1
                type: string
            required:
            - phase
            - rules
            - zoneId
            type: object
          status:
            description: CloudflareRulesetStatus defines the observed state of CloudflareRuleset
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the CloudflareRuleset's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSync:
                description: LastSync is the timestamp of the last successful synchronization
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed CloudflareRuleset
                format: int64
                type: integer
              rulesetId:
                description: RulesetID is the Cloudflare ruleset ID
                type: string
              state:
                description: State represents the current state of the ruleset
                enum:
                - Pending
                - Active
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
