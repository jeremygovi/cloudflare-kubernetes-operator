---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cloudflarezones.cloudflare.io
spec:
  group: cloudflare.io
  names:
    kind: CloudflareZone
    listKind: CloudflareZoneList
    plural: cloudflarezones
    shortNames:
    - cfz
    - cfzone
    singular: cloudflarezone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Domain
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.zoneId
      name: Zone ID
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: CloudflareZone is the Schema for the cloudflarezones API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareZoneSpec defines the desired state of CloudflareZone
            properties:
              accountId:
                description: |-
                  AccountID is the Cloudflare account ID where the zone will be created
                  If not specified, uses the CLOUDFLARE_ACCOUNT_ID environment variable
                minLength: 1
                type: string
              jumpStart:
                default: false
                description: JumpStart automatically attempts to fetch existing DNS
                  records on creation
                type: boolean
              name:
                description: Name is the domain name of the zone (e.g., example.com)
                minLength: 1
                pattern: ^([a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,}$
                type: string
              paused:
                default: false
                description: Paused indicates whether the zone is paused
                type: boolean
              type:
                default: full
                description: Type is the zone type (full, partial, secondary)
                enum:
                - full
                - partial
                - secondary
                type: string
            required:
            - name
            type: object
          status:
            description: CloudflareZoneStatus defines the observed state of CloudflareZone
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the CloudflareZone's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSync:
                description: LastSync is the timestamp of the last successful synchronization
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              nameServers:
                description: NameServers lists the Cloudflare nameservers assigned
                  to this zone
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed CloudflareZone
                format: int64
                type: integer
              originalNameServers:
                description: OriginalNameServers lists the original nameservers before
                  moving to Cloudflare
                items:
                  type: string
                type: array
              state:
                description: State represents the current state of the zone
                enum:
                - Pending
                - Active
                - Paused
                - Error
                type: string
              status:
                description: Status represents the Cloudflare zone status (active,
                  pending, etc.)
                type: string
              verificationKey:
                description: VerificationKey is used to verify domain ownership (for
                  partial zones)
                type: string
              zoneId:
                description: ZoneID is the Cloudflare zone ID
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
